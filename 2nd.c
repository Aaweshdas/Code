#include <stdio.h>

int main()
{
    int a;
    float b;
    char c;
    printf("The size of int %d",sizeof(int) );
    printf("\nThe size of float %d",sizeof(float) );
    printf("\nThe size of char %d",sizeof(char) );
}